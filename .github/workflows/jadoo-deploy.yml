name: Jadoo – Build, Test & Deploy

on:
  push:
    branches: [main]
    paths:
      - "jadoo/**"
      - ".github/workflows/jadoo-deploy.yml"
  pull_request:
    paths:
      - "jadoo/**"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/jadoo

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: jadoo
        run: bun install --frozen-lockfile

      - name: Lint & format check
        working-directory: jadoo
        run: bunx biome check .

      - name: Run tests
        working-directory: jadoo
        run: bun test test/*.test.ts

  build-and-push:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: jadoo
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: jadoo-prod
    steps:
      - uses: actions/checkout@v4

      - name: Copy compose file to server
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "143.244.150.18 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJd1UgVA1dwm/C46a5aRNn9/DmY1PnFytZMGrzL11ZYU" >> ~/.ssh/known_hosts
          echo "$DEPLOY_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh -i ~/.ssh/deploy_key deploy@143.244.150.18 "mkdir -p ~/jadoo"
          scp -i ~/.ssh/deploy_key \
            jadoo/deploy/docker-compose.yml \
            deploy@143.244.150.18:~/jadoo/
          rm ~/.ssh/deploy_key

      - name: Write config.toml to server
        uses: appleboy/ssh-action@v1
        with:
          host: 143.244.150.18
          username: deploy
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script_stop: true
          envs: JADOO_CONFIG_TOML
          script: |
            echo "$JADOO_CONFIG_TOML" > ~/jadoo/config.toml
            chmod 600 ~/jadoo/config.toml
        env:
          JADOO_CONFIG_TOML: ${{ secrets.JADOO_CONFIG_TOML }}

      - name: Deploy
        uses: appleboy/ssh-action@v1
        with:
          host: 143.244.150.18
          username: deploy
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script_stop: true
          script: |
            set -e
            cd ~/jadoo
            docker compose pull
            docker compose down --remove-orphans || true
            docker compose up -d
            docker image prune -f
            echo "--- container status ---"
            docker compose ps
